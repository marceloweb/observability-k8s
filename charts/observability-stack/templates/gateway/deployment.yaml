{{- if .Values.gateway.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.gateway.name }}
  namespace: {{ include "observability-stack.namespace" . }}
  labels:
    {{- include "observability-stack.componentLabels" (dict "component" .Values.gateway.name "type" "application" "context" .) | nindent 4 }}
spec:
  replicas: {{ .Values.gateway.replicas }}
  selector:
    matchLabels:
      {{- include "observability-stack.componentSelectorLabels" (dict "component" .Values.gateway.name "context" .) | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "observability-stack.componentSelectorLabels" (dict "component" .Values.gateway.name "context" .) | nindent 8 }}
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "{{ .Values.gateway.service.port }}"
        prometheus.io/path: "/metrics"
    spec:
      containers:
      - name: gateway
        image: "{{ .Values.gateway.image.repository }}:{{ .Values.gateway.image.tag }}"
        imagePullPolicy: {{ include "observability-stack.imagePullPolicy" . }}
        ports:
        - name: http
          containerPort: {{ .Values.gateway.service.port }}
          protocol: TCP
        env:
        {{- range $key, $value := .Values.gateway.env }}
        - name: {{ $key }}
          value: {{ $value | quote }}
        {{- end }}
        {{- if .Values.gateway.healthcheck.enabled }}
        livenessProbe:
          httpGet:
            path: {{ .Values.gateway.healthcheck.path }}
            port: http
          initialDelaySeconds: {{ .Values.gateway.healthcheck.initialDelaySeconds }}
          periodSeconds: {{ .Values.gateway.healthcheck.periodSeconds }}
          timeoutSeconds: {{ .Values.gateway.healthcheck.timeoutSeconds }}
          failureThreshold: {{ .Values.gateway.healthcheck.failureThreshold }}
        readinessProbe:
          httpGet:
            path: {{ .Values.gateway.healthcheck.path }}
            port: http
          initialDelaySeconds: 15
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        {{- end }}
        resources:
          {{- toYaml .Values.gateway.resources | nindent 10 }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}